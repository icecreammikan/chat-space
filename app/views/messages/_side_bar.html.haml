.chat-side
  .side-header
    .side-header__box
      .side-header__box__user-name
      = current_user.name
      .side-header__box__menu
        = link_to "#" do
          = icon 'fas', 'edit'
        = link_to edit_user_path(current_user) do
          = icon 'fas','cog'
  
  
  .groups
